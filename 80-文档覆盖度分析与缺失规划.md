# QEMU ARM64 源码分析文档 — 覆盖度分析与缺失规划

> 文档编号：80
> 基于当前 79 篇分析文档（分布在 15 个目录），对已覆盖内容进行盘点，
> 识别尚未涉及的重要子系统和主题，并按优先级规划后续分析方向。

---

## 一、当前覆盖状况总览

| 目录 | 文档数 | 覆盖主题 |
|------|--------|----------|
| architecture/ | 9 | QOM、执行循环、Machine创建、线程模型、事件循环、协程 |
| tcg/ | 15 | IR设计、前端翻译、后端代码生成、优化Pass、MTTCG、TLB、Plugin、Linux-user |
| arm64/ | 15 | CPU模型、ACPI、FDT、虚拟化VHE、Timer、PMU、SVE/SME、ID寄存器、系统寄存器框架、Semihosting |
| arm64/exception/ | 12 | EL切换、异常进入/返回、ERET、FGT、WFE/WFI Trap、SMC/HVC Trap、hflags |
| arm64/gic/ | 12 | Distributor/Redistributor/CPUInterface、ITS/LPI、虚拟中断、vGIC/KVM、IRQ路由 |
| arm64/mmu/ | 8 | PTW、Stage1/2、TLB、内存属性、Cache一致性、Hypervisor MMU |
| arm64/security/ | 9 | TrustZone、PAC、BTI、MTE、RME、GCS、安全状态转换 |
| arm64/tcg/ | 6 | ARM64 TCG前/后端、softmmu TLB、执行循环、原子操作、插件、TB边界 |
| arm64/spec-verify/ | 6 | Timer/指令集/SVE/PAC-BTI-MTE/内存模型/差异总汇 规范验证 |
| arm/ | 18 | AArch32异常、CP15、GIC、Cache、Timer、PMU、调试、SVE、TrustZone、VirtIO、PCI、SMMU |
| device-model/ | 19 | VirtIO、Block层、Chardev、VFIO、DMA、Timer、PMU、SMMU、PCI/PCIe、网络后端 |
| memory/ | 6 | MemoryRegion、AddressSpace、FlatView、RAMBlock、脏页追踪、IOMMU |
| debug/ | 4 | GDB Stub、断点/观察点、ARM64调试架构、规范验证 |
| monitor/ | 1 | QMP/QAPI命令分发与Schema |
| network/ | 2 | TAP/SLIRP/VirtIO-net/vhost |
| **总计** | **142** | |

---

## 二、已覆盖深度评估

### ✅ 覆盖全面（无明显缺失）

| 领域 | 评估 |
|------|------|
| GICv3 中断控制器 | 12篇覆盖完整生命周期、ITS、虚拟化、KVM、规范验证 |
| 异常/EL切换 | 12篇覆盖ERET/FGT/Trap/hflags/规范验证 |
| MMU/TLB | 8篇覆盖PTW、Stage2、属性、Cache一致性、规范验证 |
| 安全扩展 | 9篇覆盖TrustZone/PAC/BTI/MTE/RME/GCS |
| TCG核心 | 15+6=21篇覆盖IR/优化/后端/MTTCG/TLB/Plugin |
| 规范验证 | 6篇系统性对照DDI0487各章节 |

### ⚠️ 有覆盖但存在盲区

| 领域 | 已有 | 缺失点 |
|------|------|--------|
| QOM/架构 | 基本QOM分析 | **Reset/Clock框架**、**设备生命周期(realize/unrealize)** |
| 设备模型 | VirtIO/Block/PCI | **Firmware接口(pflash/UEFI)**、**GPIO/I2C/SPI** |
| 内存子系统 | Region/FlatView | **迁移时脏页同步**、**内存后端(file/hugepage)** |
| 网络 | 基本TAP/vhost | **ebpf过滤器**、**多队列细节** |
| ARM64 | 通用指令模拟 | **浮点/NEON翻译**、**Crypto扩展** |

---

## 三、完全缺失的重要子系统

按代码规模和重要性排序：

### 🔴 高优先级（核心子系统，代码量大）

| 编号 | 主题 | 源码位置 | 代码量 | 重要性说明 |
|------|------|----------|--------|-----------|
| G1 | **Migration/Live Migration** | migration/ (44个.c) | 1.3MB | QEMU最复杂子系统之一，涉及状态序列化、脏页追踪、多线程传输、COLO容灾 |
| G2 | **virt Machine 初始化** | hw/arm/virt.c + virt-acpi-build.c | ~5000行 | ARM64最核心机型，设备创建、内存布局、ACPI/FDT生成的入口 |
| G3 | **KVM加速器集成** | accel/kvm/, target/arm/kvm*.c | ~3000行 | KVM ioctl接口、vCPU运行循环、寄存器同步、设备直通 |
| G4 | **CPU启动与PSCI** | hw/arm/boot.c, target/arm/psci.c | ~1500行 | Power State Coordination Interface、多核启动、CPU热插拔 |
| G5 | **UI/Display子系统** | ui/ (62个.c) | 1.7MB | VNC/SPICE/SDL/GTK/DBus显示、图形加速、输入设备 |

### 🟡 中优先级（重要功能模块）

| 编号 | 主题 | 源码位置 | 说明 |
|------|------|----------|------|
| G6 | **Snapshot/Savevm** | migration/savevm.c, block/snapshot.c | 快照保存/恢复、VMState序列化框架 |
| G7 | **Reset/Clock框架** | hw/core/reset*.c, hw/core/clock*.c | 设备复位传播、时钟树建模 |
| G8 | **Firmware接口** | hw/arm/boot.c, pc-bios/, pflash | UEFI启动、pflash模拟、Secure Boot链 |
| G9 | **Replay/Record** | replay/ (104KB) | 确定性重放录制、调试与故障复现 |
| G10 | **icount指令计数** | accel/tcg/icount-common.c, system/cpu-timers.c | 虚拟时间、确定性执行、与replay配合 |
| G11 | **CPU热插拔** | hw/acpi/cpu.c, system/cpus.c | vCPU动态添加/移除、ACPI通知 |
| G12 | **Crypto子系统** | crypto/ (692KB) | 加密后端、TLS、密钥管理、块层加密 |
| G13 | **QEMU Guest Agent** | qga/ (576KB) | Guest内命令执行、文件操作、快照协助 |

### 🟢 低优先级（专用/外围模块）

| 编号 | 主题 | 源码位置 | 说明 |
|------|------|----------|------|
| G14 | Audio子系统 | audio/ (424KB) | 音频后端(PulseAudio/ALSA/SDL) |
| G15 | 9pfs文件系统 | hw/9pfs/ | VirtFS共享文件系统 |
| G16 | NVMe模拟 | hw/nvme/ | NVMe控制器/命名空间模拟 |
| G17 | USB子系统 | hw/usb/ | USB控制器(xHCI/EHCI)和设备 |
| G18 | CXL/内存扩展 | hw/cxl/ | Compute Express Link模拟 |
| G19 | RTC/Watchdog | hw/rtc/, hw/watchdog/ | 实时时钟、看门狗定时器 |
| G20 | GPIO/I2C/SPI | hw/gpio/, hw/i2c/, hw/spi/ | 低速总线模拟 |

---

## 四、已有文档的改进项

| 类型 | 说明 | 涉及文档 |
|------|------|----------|
| 交叉引用断裂 | 重组后部分文档内部引用路径过时 | accel→tcg迁移后的引用 |
| 重复内容 | arm/ 与 arm64/ 部分主题重叠（如Timer、GIC） | arm/11,13 vs arm64/gic/*, arm64/12 |
| 缺少代码路径图 | 部分文档纯文字描述缺少调用链可视化 | architecture/02, tcg/00 |
| 规范验证覆盖 | 已验证6大领域但缺KVM交互、Migration的验证 | arm64/spec-verify/ |

---

## 五、建议分析路线

### 路线A：补全ARM64核心（聚焦硬件模拟精确性）

```
G4(PSCI多核启动) → G2(virt Machine) → G8(Firmware/UEFI)
  → 浮点/NEON翻译 → ARM64 Crypto扩展
```

**适合目标**：理解完整ARM64虚拟机从开机到运行的全链路

### 路线B：补全QEMU平台能力（聚焦系统架构）

```
G1(Migration) → G6(Snapshot) → G3(KVM) → G9(Replay)
  → G7(Reset/Clock) → G11(CPU热插拔)
```

**适合目标**：理解QEMU作为虚拟化平台的高级特性

### 路线C：补全设备模型（聚焦I/O与外设）

```
G5(UI/Display) → G12(Crypto) → G16(NVMe)
  → G17(USB) → G14(Audio) → G20(GPIO/I2C)
```

**适合目标**：理解完整设备生态的模拟

### 路线D：混合路线（按重要性交叉）

```
G2(virt Machine) → G1(Migration) → G4(PSCI)
  → G3(KVM) → G7(Reset/Clock) → G8(Firmware)
  → 浮点/NEON → G6(Snapshot) → G5(UI)
```

**适合目标**：全面均衡补全，优先覆盖最重要缺失

---

## 六、下一步文档编号规划

| 文档号 | 计划主题 | 目录 |
|--------|----------|------|
| 81 | virt Machine初始化与设备创建 | architecture/ 或 新建 machine/ |
| 82 | Migration状态迁移框架 | 新建 migration/ |
| 83 | PSCI与多核启动 | arm64/ |
| 84 | KVM加速器集成 | 新建 kvm/ |
| 85 | Snapshot/VMState序列化 | migration/ |
| 86 | ARM64浮点/NEON翻译 | arm64/tcg/ |
| 87 | Reset/Clock框架 | architecture/ |
| 88 | Firmware/UEFI启动 | architecture/ 或 device-model/ |
| 89 | Replay确定性重放 | 新建 replay/ |
| 90 | icount与虚拟时间 | tcg/ |

---

## 七、统计摘要

- **已完成文档**：142 篇（含导航/综合文档）
- **已覆盖子系统**：~15 个大类
- **完全未覆盖的重要子系统**：~20 个（G1-G20）
- **覆盖度估算**：
  - ARM64 CPU 模拟：**~85%**（缺浮点/NEON/Crypto细节）
  - TCG 翻译引擎：**~90%**（基本完备）
  - 设备模型：**~60%**（主要设备已覆盖，外围缺失）
  - 平台能力：**~30%**（Migration/KVM/Snapshot 未覆盖）
  - QEMU 整体：**~55%**

> **结论**：ARM64 硬件模拟精确性方面覆盖优秀；QEMU 作为虚拟化平台的基础设施
> （迁移、快照、KVM、显示）覆盖不足，建议优先补全 G1-G4。
